---
phase: 16-typescript-migration
plan: 04
type: execute
wave: 4
depends_on: [16-03]
files_modified:
  - frontend/src/redux/store.ts
  - frontend/src/redux/hooks.ts
  - frontend/src/redux/slices/authSlice.ts
  - frontend/src/redux/slices/recipeSlice.ts
  - frontend/src/redux/slices/savedRecipesSlice.ts
  - frontend/src/redux/slices/mealPlanSlice.ts
  - frontend/src/redux/slices/shoppingListSlice.ts
  - frontend/src/types/api.types.ts
  - frontend/src/types/models.types.ts
autonomous: true
requirements: [NFR-6]

must_haves:
  truths:
    - "Redux store is fully typed with RootState and AppDispatch exports"
    - "Typed Redux hooks replace plain useDispatch and useSelector"
    - "All Redux slices have typed state interfaces"
    - "All async thunks are properly typed with Redux Toolkit"
  artifacts:
    - path: "frontend/src/redux/store.ts"
      provides: "Typed Redux store with RootState and AppDispatch"
      exports: ["RootState", "AppDispatch"]
    - path: "frontend/src/redux/hooks.ts"
      provides: "Pre-typed useAppDispatch and useAppSelector hooks"
      exports: ["useAppDispatch", "useAppSelector"]
    - path: "frontend/src/redux/slices/authSlice.ts"
      provides: "Typed auth slice with user state and async thunks"
      contains: "interface AuthState"
  key_links:
    - from: "frontend/src/redux/store.ts"
      to: "frontend/src/redux/hooks.ts"
      via: "RootState and AppDispatch types exported and consumed"
      pattern: "import type.*RootState.*AppDispatch.*from './store'"
    - from: "frontend/src/redux/hooks.ts"
      to: "frontend/src/components/*.tsx"
      via: "Typed hooks used in components"
      pattern: "import.*useAppSelector.*from.*redux/hooks"
---

<objective>
Convert Redux store, typed hooks, and all Redux slices from JavaScript to TypeScript with full Redux Toolkit type safety.

Purpose: Establish typed Redux state management foundation before converting React components. This ensures components can use typed selectors and dispatchers.
Output: Fully-typed Redux store with typed hooks for components to consume.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-typescript-migration/16-RESEARCH.md
@.planning/phases/16-typescript-migration/16-01-SUMMARY.md
@.planning/phases/16-typescript-migration/16-02-SUMMARY.md
@.planning/phases/16-typescript-migration/16-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Frontend Type Definitions</name>
  <files>frontend/src/types/api.types.ts, frontend/src/types/models.types.ts</files>
  <read_first>
    - backend/src/types/api.types.ts (backend API types to mirror)
    - backend/src/types/models.types.ts (backend model types to mirror)
  </read_first>
  <action>
Create frontend types directory and copy relevant type definitions from backend:

```bash
mkdir -p frontend/src/types
```

Create **frontend/src/types/api.types.ts** with API response interfaces (copied from backend):

```typescript
// Generic API response wrapper
export interface ApiResponse<T = any> {
  success: boolean;
  data?: T;
  error?: string;
  message?: string;
  token?: string;
}

// Paginated response
export interface PaginatedResponse<T> extends ApiResponse<T[]> {
  page: number;
  limit: number;
  total: number;
  totalPages: number;
}

// Auth request/response types
export interface LoginRequest {
  email: string;
  password: string;
}

export interface RegisterRequest {
  username: string;
  email: string;
  password: string;
  first_name: string;
  last_name: string;
}

export interface AuthResponse {
  success: boolean;
  token: string;
  user: User;
  error?: string;
}

// Recipe filter/search query types
export interface RecipeQueryParams {
  category?: string;
  difficulty?: string;
  minRating?: number;
  search?: string;
  sortBy?: string;
  page?: number;
  limit?: number;
}

// Ingredient search types
export interface IngredientSearchParams {
  query: string;
}
```

Create **frontend/src/types/models.types.ts** with frontend-specific model interfaces:

```typescript
// User types (matching backend but without password_hash)
export interface User {
  id: number;
  username: string;
  email: string;
  first_name: string;
  last_name: string;
  profile_picture_url?: string;
  created_at?: Date;
  updated_at?: Date;
}

// Recipe types
export interface Recipe {
  id: number;
  user_id: number;
  title: string;
  description: string;
  category: string;
  difficulty: string;
  cooking_time: number;
  servings: number;
  image_url: string;
  instructions: string;
  average_rating?: number;
  rating_count?: number;
  created_at?: Date;
  updated_at?: Date;
  ingredients?: RecipeIngredient[];
}

// Recipe ingredient
export interface RecipeIngredient {
  id: number;
  recipe_id: number;
  ingredient_id: number;
  ingredient_name: string;
  quantity: number;
  unit: string;
}

// Rating types
export interface Rating {
  id: number;
  user_id: number;
  recipe_id: number;
  rating: number;
  created_at?: Date;
}

// Saved recipe
export interface SavedRecipe {
  id: number;
  user_id: number;
  recipe_id: number;
  recipe?: Recipe;
  created_at?: Date;
}

// Meal plan
export interface MealPlan {
  id: number;
  user_id: number;
  recipe_id: number;
  week_start_date: Date | string;
  day_of_week: number;
  meal_type: string;
  recipe?: Recipe;
  created_at?: Date;
}

// Shopping list
export interface ShoppingListItem {
  id: number;
  user_id: number;
  week_start_date: Date | string;
  ingredient_name: string;
  quantity: number;
  unit: string;
  category: string;
  is_checked: boolean;
  created_at?: Date;
}

// Follower
export interface Follower {
  id: number;
  follower_id: number;
  following_id: number;
  created_at?: Date;
}

// Notification
export interface Notification {
  id: number;
  user_id: number;
  sender_id: number;
  type: string;
  message: string;
  recipe_id?: number;
  is_read: boolean;
  created_at?: Date;
}
```

These types establish the contracts between frontend components and backend API responses.
  </action>
  <acceptance_criteria>
    - frontend/src/types/ directory exists
    - frontend/src/types/api.types.ts exists with ApiResponse, AuthResponse, LoginRequest, RegisterRequest interfaces
    - frontend/src/types/models.types.ts exists with User, Recipe, Rating, SavedRecipe, MealPlan, ShoppingListItem interfaces
    - User interface does NOT include password_hash (frontend-safe)
    - All interfaces use TypeScript syntax
  </acceptance_criteria>
  <verify>
    <automated>
test -d frontend/src/types && \
test -f frontend/src/types/api.types.ts && \
test -f frontend/src/types/models.types.ts && \
grep -q "export interface User" frontend/src/types/models.types.ts && \
grep -q "export interface ApiResponse" frontend/src/types/api.types.ts && \
! grep -q "password_hash" frontend/src/types/models.types.ts && \
echo "✅ Frontend type definitions created"
    </automated>
  </verify>
  <done>Frontend type definitions created, mirroring backend API contracts without sensitive fields</done>
</task>

<task type="auto">
  <name>Task 2: Convert Redux Store and Create Typed Hooks</name>
  <files>frontend/src/redux/store.ts, frontend/src/redux/hooks.ts</files>
  <read_first>
    - frontend/src/redux/store.js (current implementation)
    - .planning/phases/16-typescript-migration/16-RESEARCH.md (Pattern 3: Redux Toolkit with TypeScript)
  </read_first>
  <action>
Rename and convert the Redux store:

```bash
cd frontend/src/redux
mv store.js store.ts
```

Convert **store.ts** to TypeScript:

```typescript
import { configureStore } from '@reduxjs/toolkit';
import authReducer from './slices/authSlice';
import recipeReducer from './slices/recipeSlice';
import savedRecipesReducer from './slices/savedRecipesSlice';
import mealPlanReducer from './slices/mealPlanSlice';
import shoppingListReducer from './slices/shoppingListSlice';

export const store = configureStore({
  reducer: {
    auth: authReducer,
    recipes: recipeReducer,
    savedRecipes: savedRecipesReducer,
    mealPlan: mealPlanReducer,
    shoppingList: shoppingListReducer
  }
});

// Infer RootState and AppDispatch types from store itself
export type RootState = ReturnType<typeof store.getState>;
export type AppDispatch = typeof store.dispatch;

export default store;
```

Create **hooks.ts** with pre-typed Redux hooks:

```typescript
import { useDispatch, useSelector } from 'react-redux';
import type { RootState, AppDispatch } from './store';

// Pre-typed hooks to use throughout the app instead of plain useDispatch/useSelector
export const useAppDispatch = useDispatch.withTypes<AppDispatch>();
export const useAppSelector = useSelector.withTypes<RootState>();
```

**Why typed hooks matter:**
- Components use `useAppDispatch()` and `useAppSelector()` instead of plain hooks
- TypeScript automatically knows the shape of state and available actions
- Autocomplete works for state selectors: `useAppSelector((state) => state.auth.user)`
- No need to manually type every useSelector call
  </action>
  <acceptance_criteria>
    - frontend/src/redux/store.ts exists (renamed from .js)
    - store.ts exports RootState and AppDispatch types
    - RootState uses ReturnType&lt;typeof store.getState&gt;
    - AppDispatch uses typeof store.dispatch
    - frontend/src/redux/hooks.ts exists (new file)
    - hooks.ts exports useAppDispatch and useAppSelector
    - Hooks use .withTypes&lt;T&gt; pattern from React Redux
  </acceptance_criteria>
  <verify>
    <automated>
test ! -f frontend/src/redux/store.js && \
test -f frontend/src/redux/store.ts && \
grep -q "export type RootState" frontend/src/redux/store.ts && \
grep -q "export type AppDispatch" frontend/src/redux/store.ts && \
test -f frontend/src/redux/hooks.ts && \
grep -q "useAppDispatch.*withTypes" frontend/src/redux/hooks.ts && \
echo "✅ Redux store and typed hooks created"
    </automated>
  </verify>
  <done>Redux store converted to TypeScript with RootState/AppDispatch exports, typed hooks created</done>
</task>

<task type="auto">
  <name>Task 3: Convert All Redux Slices to TypeScript</name>
  <files>
frontend/src/redux/slices/authSlice.ts,
frontend/src/redux/slices/recipeSlice.ts,
frontend/src/redux/slices/savedRecipesSlice.ts,
frontend/src/redux/slices/mealPlanSlice.ts,
frontend/src/redux/slices/shoppingListSlice.ts
  </files>
  <read_first>
    - frontend/src/redux/slices/authSlice.js (current implementation)
    - frontend/src/types/models.types.ts (type definitions from Task 1)
    - frontend/src/types/api.types.ts (API types from Task 1)
    - .planning/phases/16-typescript-migration/16-RESEARCH.md (Pattern 3: Redux Toolkit with TypeScript - full authSlice example)
  </read_first>
  <action>
For each Redux slice, perform these conversion steps:

**Step 1: Rename all slice files**
```bash
cd frontend/src/redux/slices
mv authSlice.js authSlice.ts
mv recipeSlice.js recipeSlice.ts
mv savedRecipesSlice.js savedRecipesSlice.ts
mv mealPlanSlice.js mealPlanSlice.ts
mv shoppingListSlice.js shoppingListSlice.ts
```

**Step 2: Convert authSlice.ts as example (apply same pattern to all):**

```typescript
import { createSlice, createAsyncThunk, PayloadAction } from '@reduxjs/toolkit';
import { User } from '../../types/models.types';
import { LoginRequest, RegisterRequest, AuthResponse } from '../../types/api.types';
import authService from '../../services/authService';

// State interface
interface AuthState {
  user: User | null;
  token: string | null;
  isAuthenticated: boolean;
  loading: boolean;
  error: string | null;
}

// Initial state with proper types
const initialState: AuthState = {
  user: null,
  token: localStorage.getItem('token'),
  isAuthenticated: !!localStorage.getItem('token'),
  loading: false,
  error: null
};

// Async thunk with proper generics: <ReturnType, InputType, { rejectValue: ErrorType }>
export const login = createAsyncThunk<
  AuthResponse,
  LoginRequest,
  { rejectValue: string }
>(
  'auth/login',
  async (credentials, { rejectWithValue }) => {
    try {
      const response = await authService.login(credentials);
      if (!response.success) {
        return rejectWithValue(response.error || 'Login failed');
      }
      return response;
    } catch (error: any) {
      const message = error.response?.data?.error || 'Unable to connect to server';
      return rejectWithValue(message);
    }
  }
);

export const register = createAsyncThunk<
  AuthResponse,
  RegisterRequest,
  { rejectValue: string }
>(
  'auth/register',
  async (userData, { rejectWithValue }) => {
    try {
      const response = await authService.register(userData);
      if (!response.success) {
        return rejectWithValue(response.error || 'Registration failed');
      }
      return response;
    } catch (error: any) {
      const message = error.response?.data?.error || 'Unable to connect to server';
      return rejectWithValue(message);
    }
  }
);

const authSlice = createSlice({
  name: 'auth',
  initialState,
  reducers: {
    logout: (state) => {
      state.user = null;
      state.token = null;
      state.isAuthenticated = false;
      state.error = null;
      localStorage.removeItem('token');
      localStorage.removeItem('user');
    },
    clearError: (state) => {
      state.error = null;
    },
    updateUser: (state, action: PayloadAction<Partial<User>>) => {
      if (state.user) {
        state.user = { ...state.user, ...action.payload };
        localStorage.setItem('user', JSON.stringify(state.user));
      }
    }
  },
  extraReducers: (builder) => {
    builder
      // Login
      .addCase(login.pending, (state) => {
        state.loading = true;
        state.error = null;
      })
      .addCase(login.fulfilled, (state, action) => {
        state.loading = false;
        state.user = action.payload.user;
        state.token = action.payload.token;
        state.isAuthenticated = true;
        localStorage.setItem('token', action.payload.token);
        localStorage.setItem('user', JSON.stringify(action.payload.user));
      })
      .addCase(login.rejected, (state, action) => {
        state.loading = false;
        state.error = action.payload || 'Login failed';
      })
      // Register
      .addCase(register.pending, (state) => {
        state.loading = true;
        state.error = null;
      })
      .addCase(register.fulfilled, (state, action) => {
        state.loading = false;
        state.user = action.payload.user;
        state.token = action.payload.token;
        state.isAuthenticated = true;
        localStorage.setItem('token', action.payload.token);
        localStorage.setItem('user', JSON.stringify(action.payload.user));
      })
      .addCase(register.rejected, (state, action) => {
        state.loading = false;
        state.error = action.payload || 'Registration failed';
      });
  }
});

export const { logout, clearError, updateUser } = authSlice.actions;
export default authSlice.reducer;
```

**Key TypeScript changes applied to ALL slices:**
1. Import PayloadAction from '@reduxjs/toolkit'
2. Import relevant types from '../../types/models.types' and '../../types/api.types'
3. Define state interface (e.g., AuthState, RecipeState, etc.)
4. Type initialState with the interface
5. Type createAsyncThunk with generics: `<ReturnType, InputType, { rejectValue: ErrorType }>`
6. Type action payloads in reducers: `action: PayloadAction<T>`
7. Type catch blocks: `catch (error: any)`
8. Export actions and reducer with proper types

**Apply this pattern to:**
- **recipeSlice.ts**: RecipeState with recipes array, loading, error, filters
- **savedRecipesSlice.ts**: SavedRecipesState with savedRecipes array
- **mealPlanSlice.ts**: MealPlanState with meal plan entries grouped by week
- **shoppingListSlice.ts**: ShoppingListState with shopping list items

For each slice:
- Define appropriate state interface
- Type all async thunks
- Type all action payloads
- Import relevant model types from frontend/src/types/
  </action>
  <acceptance_criteria>
    - All 5 slice .js files renamed to .ts
    - All slices define state interface (AuthState, RecipeState, etc.)
    - All slices type initialState with the interface
    - All createAsyncThunk calls use proper generics (ReturnType, InputType, rejectValue)
    - All action payloads use PayloadAction&lt;T&gt; type
    - All slices import types from ../../types/models.types and ../../types/api.types
    - No .js slice files remain
  </acceptance_criteria>
  <verify>
    <automated>
test ! -f frontend/src/redux/slices/authSlice.js && \
test -f frontend/src/redux/slices/authSlice.ts && \
grep -q "interface AuthState" frontend/src/redux/slices/authSlice.ts && \
grep -q "createAsyncThunk<" frontend/src/redux/slices/authSlice.ts && \
grep -q "PayloadAction" frontend/src/redux/slices/authSlice.ts && \
[ $(find frontend/src/redux/slices -name "*.ts" -type f | wc -l) -ge 5 ] && \
[ $(find frontend/src/redux/slices -name "*.js" -type f | wc -l) -eq 0 ] && \
echo "✅ All Redux slices converted to TypeScript"
    </automated>
  </verify>
  <done>All 5 Redux slices converted to TypeScript with typed state interfaces and async thunks</done>
</task>

<task type="auto">
  <name>Task 4: Verify Redux TypeScript Compilation</name>
  <files>N/A</files>
  <read_first>
    - frontend/src/redux/store.ts
    - frontend/src/redux/hooks.ts
    - frontend/src/redux/slices/authSlice.ts
  </read_first>
  <action>
Verify Redux TypeScript files compile without errors:

```bash
cd frontend
npx tsc --noEmit
```

This checks:
- Redux store type exports are correct
- Hooks are properly typed
- All slice state interfaces are valid
- Async thunks use correct generic types
- Action payloads are properly typed

Common errors to fix:
1. **Circular import errors**: Ensure hooks.ts imports types from store.ts, not vice versa
2. **createAsyncThunk generic order**: Must be `<ReturnType, InputType, ThunkApiConfig>`
3. **PayloadAction missing**: Import from '@reduxjs/toolkit'
4. **Type import issues**: Use `import type` for type-only imports if needed

If compilation succeeds, test that hooks can be imported (syntax check):

```bash
grep -r "useAppSelector" frontend/src/redux/slices/ || echo "No references yet (expected)"
```

The hooks will be used in components in the next plan (Wave 5).
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` in frontend/ exits with code 0
    - No TypeScript errors in Redux files
    - RootState and AppDispatch types export correctly
    - Typed hooks ready for component usage
  </acceptance_criteria>
  <verify>
    <automated>
cd frontend && npx tsc --noEmit 2>&1 | tee /tmp/tsc-redux-check.log && \
if grep -q "error TS" /tmp/tsc-redux-check.log; then \
  echo "❌ TypeScript errors found" && exit 1; \
else \
  echo "✅ Redux TypeScript compilation successful"; \
fi
    </automated>
  </verify>
  <done>Redux store, hooks, and slices compile successfully, ready for component integration</done>
</task>

</tasks>

<verification>
1. Frontend type definitions created mirroring backend API contracts
2. Redux store converted with RootState and AppDispatch exports
3. Typed Redux hooks (useAppDispatch, useAppSelector) created
4. All 5 Redux slices converted with state interfaces
5. All async thunks properly typed with Redux Toolkit generics
6. All action payloads typed with PayloadAction
7. Redux TypeScript compilation successful
</verification>

<success_criteria>
- frontend/src/types/ contains api.types.ts and models.types.ts
- Redux store exports RootState and AppDispatch types
- Typed hooks available for components to import
- All Redux slices have typed state interfaces
- All async thunks use proper generic types
- TypeScript compilation succeeds with no Redux errors
- Components can import and use typed Redux hooks in next phase
</success_criteria>


Note: TypeScript enabled with relaxed mode (allowJs: true, strict: false) to maintain working builds during migration. Full type safety with strict mode will be enabled in Plan 07.

<output>
After completion, create `.planning/phases/16-typescript-migration/16-04-SUMMARY.md`
</output>
