import React from 'react';
import { render, screen } from '@testing-library/react';

describe('ImageUpload', () => {
  test('placeholder - component renders', () => {
    // TODO: Implement when ImageUpload component is created
    expect(true).toBe(true);
  });

  test.todo('shows file input');
  test.todo('previews selected image');
  test.todo('validates file type');
  test.todo('validates file size');
});
